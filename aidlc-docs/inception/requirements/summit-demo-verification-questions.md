# AWS Summit デモ展示向け機能強化 - 要件確認質問

これまでの会話でスコープが大きく絞り込まれました。本ファイルでは確定事項を整理した上で、**まだ未確定の論点のみ**を質問します。

各質問の `[Answer]:` タグの後に、該当する選択肢の英字（A、B、C...）を記入してください。選択肢に合致するものがない場合は最後の「Other」を選び、内容を記述してください。記入が終わったらお知らせください。

---

## 確定済みの前提（参考・回答不要）

| 項目 | 決定 |
|---|---|
| デモのゴール | アセット/ソリューションの**認知拡大** |
| 体験形式 | 参加型＋見せる型 |
| 入力方式 | チャット（キーボード打鍵 + サジェスト返答ボタン併用） |
| 物理構成 | 体験PC 1台 → 大型モニターは**ミラーリング接続**（観客向け専用ビューは作らない） |
| 認証 | Cognito認証スキップは禁止。**共通デモユーザー1アカウントを事前ログインして使い回す**（運用で対応） |
| リセット運用 | ワンクリックリセットは不要。シナリオ再開で代替 |
| ランキング | デモでは使わない（廃止） |
| 称号・終了演出 | **不要** |
| QR持ち帰り | 不要（ブースパネルにQR掲載済み） |
| 期間 | 2週間 |

### 確定済みの実装スコープ

1. **ブース専用ショートシナリオ**（2〜3往復で決着する圧縮版シナリオ）
2. **サジェスト返答ボタン**（チャットでのタップ選択式の返答）

---

## Question 1: スコープの最終確認
上記の実装スコープ（①ショートシナリオ ②サジェスト返答ボタン）で進めてよいですか？

A) この2点で進める

B) この中から削りたいものがある（[Answer]の後に記述）

C) 追加したいものがある（[Answer]の後に記述）

X) Other (please describe after [Answer]: tag below)

[Answer]: A

---

## Question 2: サジェスト返答ボタンの生成方式
チャットの「サジェスト返答ボタン（3択など）」の選択肢をどう用意しますか？

A) シナリオに固定の選択肢を事前定義（安定・予測可能、ブース運用が読める）

B) AIがその場の文脈から動的生成（多様性は高いが、生成のブレ・レイテンシのリスク）

C) ハイブリッド（基本は固定、フリーテキスト入力欄も併設してじっくり試したい人に対応）

X) Other (please describe after [Answer]: tag below)

[Answer]: B

---

## Question 3: サジェスト返答ボタンの適用範囲
サジェスト返答ボタンは、デモ専用にしますか、本番でも使えるようにしますか？

A) デモ専用（ブース用ショートシナリオでのみ表示。フラグ切り替え）

B) 本番でも選択可能にする（シナリオ設定でON/OFF。汎用機能として実装）

X) Other (please describe after [Answer]: tag below)

[Answer]: B

---

## Question 4: アバター感情表現の強化の適用範囲
感情表現の強化（表情・ジェスチャーの誇張）を、どう適用しますか？

A) 表現強度をパラメータ化し、レベルで調整可能にする（本番は中程度に強化、デモは最大に誇張）。営業練習のリアリティとデモの視認性を両立（推奨）

B) 本番のアバター表現デフォルト自体を引き上げる（本番もデモも同じ強化表現。実装は単純だが、商談練習としてのリアリティは下がる可能性）

C) デモ専用モードにとどめる（本番の表現は現状維持、デモ時のみ誇張フラグON）

X) Other (please describe after [Answer]: tag below)

[Answer]: B

---

## Question 5: ブース専用ショートシナリオの作り方
2〜3往復で決着するショートシナリオをどう用意しますか？

A) 既存のシナリオ管理機能でデータとして新規作成する（コード変更不要。ゴール条件や初期メトリクスを調整）

B) デモ専用に会話ターン数の上限や終了条件をコードで制御する仕組みを追加する（実装あり）

C) 既存シナリオを流用し、運用（スタッフが短めに切り上げる）で対応する

X) Other (please describe after [Answer]: tag below)

[Answer]: A. 【デモ】AWSクラウド移行提案 - 初回商談　を改造します。

---

## Question 6: 既存の本番（認証済みユーザー向け）機能への影響範囲
デモ向けの変更を、既存の本番機能とどう共存させますか？

A) フラグ/環境変数/シナリオ設定で切り替え、本番機能への影響を最小化（既存コードへの変更を抑える）

B) デモ専用の別ルート/別ページとして追加（既存ページは一切変更しない）

C) 既存の会話フローを拡張して共通化（コード重複を避けるが、既存への影響あり）

X) Other (please describe after [Answer]: tag below)

[Answer]: "デモ向け" にはしません。デモ用のためだけに特別な実装をするつもりはありません。

---

## Question 7: Security Extension（セキュリティ拡張ルール）
このプロジェクトでセキュリティ拡張ルールを強制適用しますか？

A) Yes — すべてのSECURITYルールをブロッキング制約として強制（本番グレードのアプリケーション向け推奨）

B) No — SECURITYルールをスキップ（PoC、プロトタイプ、実験的プロジェクト向け）

X) Other (please describe after [Answer]: tag below)

[Answer]: B

---

## Question 8: Property-Based Testing Extension（プロパティベーステスト拡張ルール）
このプロジェクトでプロパティベーステスト（PBT）ルールを強制適用しますか？

A) Yes — すべてのPBTルールをブロッキング制約として強制（ビジネスロジック、データ変換、シリアライゼーション、ステートフルなコンポーネントを持つプロジェクト向け推奨）

B) Partial — 純粋関数とシリアライゼーションのラウンドトリップのみPBTルールを適用（アルゴリズム的複雑性が限定的なプロジェクト向け）

C) No — PBTルールをスキップ（単純なCRUDアプリ、UIのみのプロジェクト、ビジネスロジックの薄い統合層向け）

X) Other (please describe after [Answer]: tag below)

[Answer]: C
